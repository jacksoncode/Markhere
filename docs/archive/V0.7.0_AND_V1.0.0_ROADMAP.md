# 🚀 Markhere v0.7.0 & v1.0.0 完整开发路线图

**日期**: 2026-06-07  
**当前版本**: v0.6.0-dev  
**下一里程碑**: v0.7.0 & v1.0.0  

---

## 📋 核心目标

| 版本 | 核心主题 | 预计工作量 | 完成时间 |
|------|----------|------------|----------|
| **v0.7.0** | 插件生态 & 导出增强 | ~10 天 | 2026-06-20 |
| **v1.0.0** | 正式版 & 移动端 | ~15 天 | 2026-07-05 |

---

## 🎯 v0.7.0 - 插件生态 & 导出增强（高优先级）

### 🔴 P0 - 必须完成

#### 1. 插件市场完整生态
**竞品参考**: VS Code Extension Marketplace / Obsidian Plugin Repository
**目标**: 建立完整的插件发现、安装、管理、交易生态系统

**新增文件**:
```
src/services/PluginRegistry.ts         - 插件注册中心
src/services/PluginMarketplaceAPI.ts    - 插件市场 API
src/components/PluginMarketplace/
  PluginMarketplace.tsx              - 插件市场主界面
  PluginCard.tsx                     - 插件卡片
  PluginDetail.tsx                   - 插件详情页
  InstalledPlugins.tsx               - 已安装插件
src/store/pluginMarketplaceStore.ts     - 插件市场状态
e2e/plugin-marketplace.spec.ts         - 插件市场 E2E 测试
```

**功能要求**:
- [ ] 插件搜索（关键词/标签/作者）
- [ ] 插件分类（编辑器增强/主题/协作/AI/导出）
- [ ] 插件详情页（描述/版本历史/评分/评论）
- [ ] 一键安装/卸载/更新插件
- [ ] 插件权限管理
- [ ] 插件自动更新检查
- [ ] 插件沙箱安全机制
- [ ] 官方插件认证标识
- [ ] 插件评分和评论系统
- [ ] 插件安装数量排行榜

**技术实现要点**:
```typescript
// 插件清单格式
interface PluginManifest {
  id: string;
  name: string;
  version: string;
  author: string;
  description: string;
  icon?: string;
  category: string;
  tags: string[];
  permissions: string[];
  main: string;
  repo?: string;
  homepage?: string;
  screenshots?: string[];
}

// 插件注册表
class PluginRegistry {
  static registry: Map<string, PluginManifest> = new Map();
  
  static async searchPlugins(query: string): Promise<PluginManifest[]>;
  static async getPluginDetail(id: string): Promise<PluginManifest | null>;
  static async installPlugin(id: string, version?: string): Promise<void>;
  static async uninstallPlugin(id: string): Promise<void>;
  static async checkForUpdates(): Promise<Array<{ id: string; current: string; latest: string }>>;
}
```

#### 2. 更多导出格式增强
**竞品参考**: Notion / Typora / iA Writer
**目标**: 支持多种专业文档格式的高质量导出

**新增文件**:
```
src/services/ExportEnhanced.ts         - 增强导出服务
src/components/Export/
  ExportDialogAdvanced.tsx          - 高级导出对话框
  ExportPreview.tsx                 - 导出预览
  ExportTemplates.tsx               - 导出模板库
src/templates/
  pptx-template.ts                  - PPTX 生成模板
  latex-template.ts                 - LaTeX 生成模板
e2e/export-advanced.spec.ts          - 高级导出 E2E 测试
```

**功能要求**:
- [ ] PPTX 导出（幻灯片格式，reveal.js 风格）
- [ ] LaTeX 导出（学术论文格式）
- [ ] Markdown 增强导出（frontmatter 标准化）
- [ ] 自定义导出模板（模板市场）
- [ ] 导出预览和对比
- [ ] 批量导出功能
- [ ] 导出质量设置（图片压缩/字体嵌入）
- [ ] 导出历史记录

**技术实现要点**:
```typescript
// 导出选项
interface ExportOptions {
  format: 'pdf' | 'docx' | 'html' | 'pptx' | 'latex' | 'md' | 'epub';
  template?: string;
  quality?: 'low' | 'medium' | 'high';
  includeFrontmatter?: boolean;
  compressImages?: boolean;
  embedFonts?: boolean;
}

// PPTX 生成
class PPTXExporter {
  static async export(markdown: string, options: ExportOptions): Promise<Blob> {
    // 将 Markdown 转换为 PPTX 幻灯片
    // 每个 H1/H2 作为新幻灯片开始
    // 支持代码块/图片/表格/数学公式
  }
}

// LaTeX 生成
class LaTeXExporter {
  static async export(markdown: string, options: ExportOptions): Promise<string> {
    // 支持学术论文格式
    // 支持标题/摘要/关键词/章节/参考文献
  }
}
```

### 🟡 P1 - 重要功能

#### 3. Canvas 协作白板
**目标**: 让多个用户可以在同一块 Canvas 上实时协作

**新增文件**:
```
src/services/CanvasCollaboration.ts  - Canvas 协作服务
src/hooks/useCanvasWebSocket.ts     - Canvas WebSocket Hook
```

**功能要求**:
- [ ] 协作者光标显示（不同颜色）
- [ ] 实时同步卡片和连接线
- [ ] 版本历史记录
- [ ] 冲突解决机制
- [ ] Canvas 权限管理（只读/编辑）

#### 4. 写作历史和版本管理增强
**目标**: 更完善的写作历史和版本对比功能

**新增文件**:
```
src/services/VersionHistory.ts         - 版本历史服务
src/components/VersionHistory/
  VersionTimeline.tsx               - 版本时间线
  VersionDiff.tsx                   - 版本对比
e2e/version-history.spec.ts            - 版本历史 E2E 测试
```

**功能要求**:
- [ ] 自动保存版本（30s 间隔）
- [ ] 手动标记版本（重要里程碑）
- [ ] 版本对比（高亮差异）
- [ ] 版本还原功能
- [ ] 版本注释

### 🟢 P2 - 完善功能

#### 5. 更多 AI 功能增强
- [ ] 代码自动补全和优化
- [ ] 表格数据智能分析
- [ ] 思维导图自动生成
- [ ] 写作风格一致性检查
- [ ] 语法和拼写检查增强

---

## 🎉 v1.0.0 - 正式版 & 移动端（里程碑）

### 🔴 P0 - 正式版必备功能

#### 1. 性能优化最终版
**目标**: 确保 Markhere 性能达到工业级标准

**需要优化的点**:
- [ ] 大文件加载速度（100MB < 10s）
- [ ] 滚动流畅度（60fps）
- [ ] 启动时间（< 1s）
- [ ] 内存占用优化（< 500MB 常态）
- [ ] CPU 占用优化（< 5% 闲置）

**新增文件**:
```
src/services/PerformanceOptimizer.ts   - 性能优化器
e2e/performance-final.spec.ts         - 性能验收测试
```

#### 2. 桌面应用稳定性强化
**目标**: 零闪退、零数据丢失

**功能要求**:
- [ ] 自动保存保险机制（断电不丢数据）
- [ ] 崩溃恢复（重启后自动恢复）
- [ ] 文件备份（云端/本地双重备份）
- [ ] 数据完整性校验
- [ ] 紧急恢复模式

**新增文件**:
```
src/services/DataRecovery.ts         - 数据恢复服务
src-tauri/src/data_recovery.rs       - Rust 数据恢复层
```

### 🟡 P1 - 移动端应用

#### 3. iOS/Android 原生应用
**竞品参考**: Notion Mobile / Obsidian Mobile / iA Writer
**目标**: 提供完整的移动端 Markdown 体验

**技术栈**:
- **方案 A**: Capacitor (基于现有 Web 代码)
- **方案 B**: React Native (高性能原生)
- **方案 C**: Tauri Mobile (官方支持的移动端方案)

**推荐方案**: **Tauri Mobile**（与现有代码一致性最高）

**新增目录结构**:
```
src-tauri-ios/
  Capabilities/
  Sources/
  Info.plist
  ...
src-tauri-android/
  app/
  build.gradle
  ...
src-mobile/
  components/
    MobileEditor.tsx
    MobileToolbar.tsx
    MobileSidebar.tsx
  hooks/
    useMobileGesture.ts
  styles/
    mobile.css
e2e/mobile/
  ios.spec.ts
  android.spec.ts
```

**移动端功能要求**:
- [ ] 离线可用
- [ ] 触控优化（手写/滑动/手势）
- [ ] 相机集成（拍照插入）
- [ ] 语音输入集成
- [ ] 手机/平板适配
- [ ] 云端同步（iOS iCloud / Android Google Drive）
- [ ] 深色/浅色模式自动切换
- [ ] 移动端专属键盘快捷键栏

**技术实现要点**:
```typescript
// 移动端检测 Hook
function useMobile() {
  const isMobile = window.innerWidth < 768;
  const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent);
  const isAndroid = /Android/.test(navigator.userAgent);
  
  return { isMobile, isIOS, isAndroid, platform: isIOS ? 'ios' : isAndroid ? 'android' : 'web' };
}

// 移动端工具栏
function MobileToolbar() {
  return (
    <div className="mobile-toolbar">
      {/* 快捷格式按钮 */}
      <button>Bold</button>
      <button>Italic</button>
      <button>Link</button>
      {/* 语音输入按钮 */}
      <button onClick={startVoiceInput}>🎤</button>
      {/* 相机按钮 */}
      <button onClick={openCamera}>📷</button>
    </div>
  );
}
```

### 🟢 P2 - 正式版完善

#### 4. 完整的用户手册
**文件**: `docs/USER_MANUAL.md` (完整版)
**内容**:
- [ ] 快速入门指南
- [ ] 所有功能详解
- [ ] 快捷键速查表
- [ ] 常见问题解答
- [ ] 高级技巧和窍门
- [ ] 插件开发指南

#### 5. 多语言完善
**目标**: 完善多语言支持，至少支持 5 种语言
- [ ] English (en-US) - 英语
- [ ] 中文 (zh-CN) - 简体中文
- [ ] 日本語 (ja-JP) - 日语
- [ ] 한국어 (ko-KR) - 韩语
- [ ] Français (fr-FR) - 法语

**新增文件**:
```
src/i18n/locales/
  fr-FR.json
  ja-JP.json
  ko-KR.json
  ...
src/i18n/languageService.ts  - 语言加载服务
```

---

## 📊 实施优先级和时间线

### 第一周 (v0.7.0 - 前半部分) - 插件基础
- 🔴 P0-1: 插件注册表和基础架构
- 🔴 P0-1: 插件安装和卸载功能
- 🟡 P1-3: Canvas 协作基础

### 第二周 (v0.7.0 - 后半部分) - 导出和完善
- 🔴 P0-2: PPTX/LaTeX 导出功能
- 🔴 P0-1: 插件市场完整界面
- 🟢 P2-5: AI 功能增强

### 第三周 (v1.0.0 - 前半部分) - 性能和稳定
- 🔴 P0-1: 性能优化最终版
- 🔴 P0-2: 数据恢复和稳定性

### 第四周至第五周 (v1.0.0 - 后半部分) - 移动端
- 🟡 P1-3: iOS/Android 应用开发
- 🟢 P2-4: 用户手册完成
- 🟢 P2-5: 多语言完善

---

## ✅ 验收标准

### v0.7.0 验收
- [ ] 插件市场可用，可以安装和卸载插件
- [ ] PPTX/LaTeX 导出功能正常工作
- Canvas 协作功能可用
- 所有测试通过
- 性能达标

### v1.0.0 验收
- [ ] 桌面应用稳定性达到 99.9%
- iOS/Android 应用在对应应用商店上架
- 性能指标全部达标
- 完整的用户手册和多语言支持
- 零已知严重 bug

---

## 📝 关键技术依赖

### v0.7.0
- `pptxgenjs` - PPTX 生成
- `latex.js` - LaTeX 渲染
- `ws` - WebSocket（Canvas 协作）
- `lz-string` - 数据压缩

### v1.0.0
- `@tauri-apps/api` (mobile) - Tauri 移动端 API
- `expo-speech` 或原生 API - 语音输入
- 设备相机 API - 相机集成
- 云端存储 SDK - iCloud/Google Drive

---

## 🎊 最终目标

v1.0.0 发布后，Markhere 将成为:
- ✅ 国产高端 Markdown 编辑器
- ✅ 本地优先 + AI 增强
- ✅ 跨平台（Windows/macOS/Linux/iOS/Android）
- ✅ 插件生态完善
- ✅ 对标 Notion/Obsidian 的功能完整性

**版本口号**: "一个笔记应用，无限可能" 🚀
